<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/onehub_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/onehub_app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CatalentOneHubNoActionBar">
        <!-- WelcomeOnboarding starts here -->
        <activity
            android:name=".presentation.ui.WelcomeActivity"
            android:exported="false"
            android:label="@string/title_activity_Welcome"
            android:theme="@style/Theme.CatalentOneHubNoActionBar" />
        <!-- LAUNCHER starts here -->
        <activity
            android:name=".presentation.ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.CatalentOneHubNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CatalentOneHubNoActionBar" />
        <activity
            android:name="com.salesforce.androidsdk.ui.LoginActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.CatalentOneHubNoActionBar"
            tools:replace="android:theme">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="mobile"
                    android:path="/oauth/onehub"
                    android:scheme="fieldagentapp" />
            </intent-filter>
        </activity>

        <!-- WorkManager — Disable default initializer for custom Hilt Configuration -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>
    </application>

</manifest>